class Main {
  function void main() {
    var Game game;
    var Player p1;
    var Player p2;

    let game  = Game.new();
    let p1 = Player.new("Player1", game);
    let p2 = Player.new("Player2", game);


    do Output.printString(p1.getName());
    do Output.printString(p2.getName());

    return;
  }
}
