class Main {
  function void main() {
    var Session session;

    let session = Session.new();

    while(session.getIsContinue()) {
      do session.start();
    }

    do Output.printString("Ending!!!!!!!");
    return;
  }
}
